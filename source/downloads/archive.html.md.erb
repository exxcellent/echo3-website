---
title: Download archive
---

## Archived Releases

This page contains non-current Echo3 releases. Visit the [download page](/downloads) to
get the latest current release. 

<table class="table table-striped">
	<thead>
		<tr>
			<th>Date</th>
			<th>Version</th>
			<th>Release</th>
			<th>Source code</th>
		</tr>
	</thead>
	<tbody>
	<% archived_releases.archive.each do |entry| %>
		<tr>
			<td><%= entry.date %></td>
			<td><%= entry.version %></td>
			<td><% if entry.zip_url %><a href="<%= entry.zip_url %>">zip</a><% end %></td>
			<td><% if entry.tar_url %><a href="<%= entry.tar_url %>">tar.gz</a><% end %></td>
		</tr>
	<% end %>
	</tbody>
</table>
